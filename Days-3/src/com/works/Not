while (read.hasNext()) {
            System.out.println(read.nextLine());
        }
        System.out.println("----------");
while (read.hasNext()) {
            System.out.println(read.nextLine());
        }

**Burada iki defa while yapmaz. Yukarıdaki çalıştıktan sonra aşağıda devam etmez.

**Tüm değişkenler Object'in akrabasıdır diye düşün.
